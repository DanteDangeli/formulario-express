«<!DOCTYPE html>
<html lang="es">

	<!-- todos los productos -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Todos los usuarios</h2>
			</div>
		<% for(let i = 0; i < users.length; i++) { %>
			<div class="col-12 col-sm-6 col-lg-3">
				<section class="product-box">
					<a href="/products/detail/<%= users[i].id %>">
						<article class="product-box_data">
							<h2>$ <%= users[i].first_name %> </h2>
							<span> <%= users[i].last_name %></span>
							<p> <%= users[i].email %></p>
							<a href="/products/edit/" class="action-button edit">EDITAR PRODUCTO</a>
						<form action="/eliminarUsuario/<%= users.email %>" method="delete" style="display: inline-flex">
							<button type="submit" class="action-button delete">ELIMINAR</button>
						</form>
						</article>
					</a>
				</section>
				<br>
				<br>
			</div>
		<% } %>
		
		</div>
	</div>
</body>	
</html>